
class Config:
    """
    Use to store the arguments of all the functions in the package Experiment_Engine. If a function requires arguments,
    it's definition will be: func(config), where config has all the parameters needed for the function.
    """
    def __init__(self):
        pass
